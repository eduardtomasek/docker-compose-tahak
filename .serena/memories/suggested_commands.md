Useful commands for this project on Darwin:
- `ls -la` — inspect repo contents
- `rg --files` — list files quickly
- `rg "pattern" docker-compose-tahak.html` — find HTML/CSS/JS fragments
- `sed -n '1,220p' docker-compose-tahak.html` — inspect parts of the single-file page
- `open docker-compose-tahak.html` — open the page locally in a browser if needed
There are currently no detected project-specific test, lint, or build commands.